# gcoc
## How to work this repo
Swith to `main`, make your changes. Then run `npm run deploy`. This will predeploy the changes, and then deploy the artifacts to gh-pages which is picked up by GitHub Pages.